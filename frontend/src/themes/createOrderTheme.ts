import { alpha, styled } from "@mui/system"
import { Grid } from "@mui/material"

const OrderMainContainer = styled(Grid)(({ theme }) => ({}))

const OrderMainItem = styled(Grid)(({ theme }) => ({
  backgroundColor: "rgba(243,241,224,1)",
}))

export { OrderMainContainer, OrderMainItem }
